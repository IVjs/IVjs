// In this file, you can use the lib and test things out on the browser page

import '../styles/base.scss';

import { IV } from './lib/index';

const el = document.getElementById('nodes-container');

IV.defineNode('firstNode');
// .etc()
